{
  "name": "worldq-gateway",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Provider-agnostic zero-paid-API WORLDQ execution gateway for AGENTROPOLIS",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
