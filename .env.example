# WORLDQ zero-paid-API competition mode
PORT=8787
ALLOWED_ORIGINS=https://agentropolis-city-of-agents.github.io,http://localhost:5173,http://127.0.0.1:5173
WORLDQ_PROVIDER=system
WORLDQ_MODEL=worldq-system
ENVELOPE_VERSION=worldq-envelope-v1

# Optional future provider adapters may use their own keys, but none are required.
# Never put server secrets in VITE_* variables.
